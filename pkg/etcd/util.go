package etcd

func genKey(raw string) stringWrapper {
	//TODO
	return stringWrapper{
		raw:       raw,
		processed: raw,
	}
}

func genValue(raw string) stringWrapper {
	//TODO
	return stringWrapper{
		raw:       raw,
		processed: raw,
	}
}
